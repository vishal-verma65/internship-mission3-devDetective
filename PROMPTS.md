<!--* prompts for handling REST API efficiently -->

What are the best practices for handling REST API calls efficiently in a plain js project, including caching, debouncing, and error handling?

How can I reduce redundant API calls in my js app when the same data is requested multiple times in a short period?

I am working with github api for fetching user details and then showing in my project, so what should be the flow to make it work smoothly.


<!--* prompt for toggle js file generation -->

Create a simple theme toggler for my application, tech stack is html, css and js. Also i am suing css custom color variables so the toggler should be according to that, keep it simple and optimized.


<!-- *build and preview issue -->

I am using multiple js files and calling them in the html file directly with along side the main.js script, and when running in localhost, it is running smoothly but after running the build command, in preview mode, the application features are not working, so should i be calling them all in the main.js file.